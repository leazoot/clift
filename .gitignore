# Rust
/target/
**/*.rs.bk
*.pdb

# macOS
.DS_Store

# Local environment
.env
.env.local

# Cloudflare relay (relay/cloudflare)
relay/cloudflare/node_modules/
relay/cloudflare/.wrangler/
relay/cloudflare/worker-configuration.d.ts

# SBOMs are generated by the release workflow; a local `cargo cyclonedx` run
# writes one into every crate directory.
*.cdx.json
