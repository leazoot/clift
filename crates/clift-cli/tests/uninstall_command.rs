//! Entry point for the uninstall suite.
//!
//! The suite lives at `tests/e2e/uninstall.rs` and is compiled here because
//! this is the crate that produces the binary it drives.

#[path = "../../../tests/e2e/uninstall.rs"]
mod uninstall;
