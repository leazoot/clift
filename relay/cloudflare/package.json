{
  "name": "clift-relay-cloudflare",
  "version": "0.1.0",
  "private": true,
  "description": "Clift's relay as a Cloudflare Worker: holds ciphertext for minutes, hands it out once, forgets it",
  "license": "Apache-2.0",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260902.1",
    "typescript": "^7.0.2",
    "wrangler": "^4.128.0"
  }
}
